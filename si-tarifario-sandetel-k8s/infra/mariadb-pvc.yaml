apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb
  namespace: tarifario
  labels:
    app: mariadb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
